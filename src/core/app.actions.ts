import { Action } from '@ngrx/store';

export const QUIT_ACTION: Action = { type: '[FM] Quit App' };
export const RESET_ACTION: Action = { type: '[FM] Reset App' };
export const LOGOUT_ACTION: Action = { type: '[FM] Logout App' };
